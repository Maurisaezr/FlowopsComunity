# Local path containing the FlowOps/Antigravity application source.
# This directory is mounted read-only at /workspace inside the container.
FLOWOPS_SOURCE_DIR=./flowops-src

# Host port exposed by Docker Compose.
FLOWOPS_PORT=5000
